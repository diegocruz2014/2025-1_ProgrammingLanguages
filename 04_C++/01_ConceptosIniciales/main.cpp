// La biblioteca iostream se utiliza para entrada y salida por consola
// e incluye la clase string
#include <iostream> 
using namespace std;

// Función principal
int main()
{
    // Declaración de variable
    int a;
    cout << "Hola mundo!" << endl; // cout imprime en consola
    // Asignación del valor en consola a la variable
    cin >> a; // cin otorga a la variable lo que se escribe en consola 
    string cadena;
    return 0;
}